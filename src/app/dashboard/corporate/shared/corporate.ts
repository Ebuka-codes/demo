export interface Corporate {
  id: string;
  name: string;
  address: string;
  phone: string;
  email: string;
  code: string;
  hmCode: string;
  logo: string;
}

export interface file {
  base64String: string;
  fileName: string;
}
